const fs= require('fs');


class TextParser{
    constructor(filePath)
    {
        this._filePath=filePath;
    }

    readTokens(textFile, options){
        let linesArr=record.toString().split('\n');
        if(options!=undefined){
            
        }

        return linesArr;
    }
}

module.exports.TextParser=TextParser;
 